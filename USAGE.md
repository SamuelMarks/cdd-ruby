# Usage

`cdd-ruby` provides a CLI to convert OpenAPI to Ruby code, and vice versa.

```bash
cdd-ruby from_openapi -i spec.json
cdd-ruby to_openapi -f path/to/code
cdd-ruby to_docs_json --no-imports --no-wrapping -i spec.json
```
